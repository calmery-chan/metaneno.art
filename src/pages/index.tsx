import { NextPage } from "next";

const Index: NextPage = () => (
  <div className="grid w-full h-full place-items-center">
    <img src="/construction.png" style={{ maxWidth: `512px` }} />
  </div>
);

export default Index;
